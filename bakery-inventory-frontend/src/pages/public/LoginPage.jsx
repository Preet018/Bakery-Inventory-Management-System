import React from 'react';
import { CustomerLoginPage } from './CustomerLoginPage';

/**
 * LoginPage: Default entry point for /login which loads the Customer Login portal.
 */
export const LoginPage = () => {
  return <CustomerLoginPage />;
};
